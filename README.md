# MonopolyJava

A simple text-based implementation of the board game Monopoly using Java.

The game is run using maven and can be started by running `mvn clean compile exec:java` in the terminal. The game is still in development and is not yet fully functional.